# Observability

v0 scaffold signals:

- **Status bar** — project name + ontology mode, or “binding missing”
- **Error toasts** — open/query/verb/load-ontology failures
- **No telemetry** shipped in scaffold

Future: opt-in usage events for verb/query success rates; never send graph contents or project paths by default.
