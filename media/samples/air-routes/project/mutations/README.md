# Graph mutations

`seed-air-routes.cypher` is generated when the sample project is created. It is
the exact mutation GraphForge executes to materialize the vendored CSV data.

Keep additional project-backed mutations in this directory and run them from a
Cypher editor. The extension does not hide sample mutations in session state.
